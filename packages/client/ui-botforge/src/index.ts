/**
 * Host loader entry for the browser Employees plugin.
 * @module @deepseek-ai/dsh-client-ui-botforge
 */

/** Host plugin body — no host-side behavior for this surface plugin. */
export function apply(): void {}
